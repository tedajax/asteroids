ffi = require("assets/scripts/api")

Instantiate = ffi.C.instantiate
InstantiateAt = ffi.C.instantiate_at
Destroy = ffi.C.destroy

entity = 0

start = function(entity) end
update = function(dt) end
render = function() end